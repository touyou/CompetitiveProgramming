#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;
int t, n;
int main() {
    scanf("%d", &t);
    for (int i=0; i<t; i++) {
        scanf("%d", &n);
        string str[n];
        for (int j=0; j<n; j++) {
            cin >> str[j];
        }
        
    }
}
