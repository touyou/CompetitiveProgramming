#include <iostream>
#include <vector>
typedef long long ll;
typedef vector<ll> vl;
typedef vector<vl> vvl;
using namespace std;
int main() {
	int n;
	while (cin >> n&&n!=0) {
		vl input(2);
		vll map;
		while(cin >> input[0] >> input[1]&&input[0]!=0&&input[1]!=0) map.push_back(input);
		
	}
}
