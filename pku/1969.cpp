// #include <cstdio>

// int main() {
	// int n;
	// while (scanf("%d", &n)!=EOF) {
		// int r1 = 1, r2 = 1;
		// int temp = n, cnt = 1;
		// if (n == 2) {
			// printf("TEAM 2 IS 1/2\n");
			// continue;
		// }
		// while (temp>0) temp -= cnt++;
		// printf("%d %d\n", temp, cnt);
		// if (temp+cnt == n) {
			// cnt--;
			// if (cnt%2 == 0) {
				// r1 = cnt;
			// } else {
				// r2 = cnt;
			// }
		// } else {
			// cnt--;
			// temp += cnt;
			// if (cnt%2 == 0) {
				// r1 = temp;
				// r2 = cnt - temp + 1;
			// } else {
				// r1 = cnt - temp + 1	;
				// r2 = temp;
			// }
		// }
		// printf("TEAM %d IS %d/%d\n", n, r1, r2);
	// }
// }