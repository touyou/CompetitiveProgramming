#include <bits/stdc++.h>
using namespace std;
int n, d;
int x[1000], y[1000];
int main() {
    while (scanf("%d%d",&n,&d)&&(n!=0||d!=0)) {
        for (int i=0; i<n; i++) scanf("%d%d",&x[i],&y[i]);
    }
}
