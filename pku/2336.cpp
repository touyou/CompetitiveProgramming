#include <cstdio>
#include <vector>
using namespace std;
int main() {
	int c; scanf("%d",&c);
	for (int ix=0; ix<c; ix++) {
		int n,t,m; scanf("%d %d %d",&n,&t,&m);
		vector<int> cars(m);
		for (int i=0; i<m; i++) scanf("%d",&cars[i]);
		sort(cars.begin(),cars.end());
		int cnt=0,time=0;
		while (true) {
			
		}
	}
}
