#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using pll = pair<ll, ll>;
int main() {
    int n; scanf("%d", &n);
    vector<pll> vertex;
   // bool used[100000];
    for (int i = 0; i < n; i++) {
        ll a, b; cin >> a >> b;
        vertex.push_back(pll(a, b));
     //   used[i] = false;
    }
    
}
