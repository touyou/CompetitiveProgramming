#include <bits/stdc++.h>
using namespace std;

int main() {
    string ciph = "ruoYced_ehpigniriks_i_llrg_stae";
    for (int key = 1; key < ciph.length(); key++) {
//        for (int i = key - 1; i <= ciph.length(); i++) {
//            for (int j = i; j >= i - key + 1; j--) printf("%c", ciph[j]);
//        }
//        puts("");
       
    }
}
