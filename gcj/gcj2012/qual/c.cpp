#include <cstdio>
#include <sstream>
#include <vector>
using namespace std;
typedef long long ll;
ll solve() {
    int a, b; scanf("%d%d",&a,&b);
    ll res=0;
    for (int i=a; i<=b; i++) {
    }
    return res;
}
int main() {
    int t; scanf("%d",&t);
    for (int ix=1; ix<=t; ix++) {
        ll res=solve();
        printf("Case #%d: %lld\n",ix,res);
    }
}